/**
 * SVG 图标库统一尺寸约定。
 *
 * - 所有保存进图标库的 SVG 都使用 100×100 的标准 viewBox；
 * - 实际图形默认保留少量安全边距，避免描边/圆角贴边被裁切；
 * - 放到矢量拼接画布或桌面贴纸时，使用统一的 128px 默认视觉尺寸。
 */
export const VECTOR_ICON_CANONICAL_SIZE = 100;
export const VECTOR_ICON_CANONICAL_CONTENT_SIZE = 88;
export const VECTOR_ICON_DEFAULT_DISPLAY_SIZE = 128;
